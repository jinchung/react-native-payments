{
  "scripts": {
    "precommit": "lint-staged"
  },
  "devDependencies": {
    "lerna": "^3.20.2",
    "lint-staged": "^4.0.0",
    "prettier": "^1.4.4"
  },
  "lint-staged": {
    "*.js": [
      "prettier --single-quote --write",
      "git add"
    ]
  }
}
